import { TransformParams } from '@/types/types';

const initialTransformParams: TransformParams = {
    filter: { color: [], size: [], price: [], discount: [] },
    sort: '',
    search: '',
    category: '',
};

export default initialTransformParams;
